1.证书/certs/client目录
sudo mkdir -p /certs/client

# 设置权限（根据您的 runner 运行用户）
# 如果 runner 以 gitlab-runner 用户运行：
sudo chown -R gitlab-runner:gitlab-runner /certs/client

# 如果您不确定运行用户，可以查看当前 runner 进程：
ps aux | grep gitlab-runner

# 或者设置更宽松的权限（不推荐用于生产环境）：
sudo chmod -R 777 /certs/client



2.缓存Cache目录
sudo mkdir -p /cache

sudo chown -R gitlab-runner:gitlab-runner /cache
